document.addEventListener( 'DOMContentLoaded', () => {
    const splide = new Splide ('.splide', {
        type: 'loop',
        // perPage: 1,
        // pagination: false,
        arrows: false,
        autoplay: true,
        // wheel: true.
        // right: true,
        // padding: '1rem'
        
    }).mount()
} );